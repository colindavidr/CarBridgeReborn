#!/bin/sh
# CarBridgeReborn postinst - minimal + non-blocking (v3.74.0).
# NOTE: never call NathanLR injection from here - it blocks headless and wedges dpkg.
/var/jb/usr/bin/cbr-refresh-prefs 2>/dev/null || /usr/bin/cbr-refresh-prefs 2>/dev/null || true
echo "[CBR] Installed. Enable your bridged apps once in NathanLR (Inject into Apps) - injection persists across resprings and CBR updates."
exit 0
